## 1. UI 修改

- [x] 1.1 在学校表格每行末尾添加"编辑"按钮列
- [x] 1.2 修改网址列显示为纯文本（移除 `<a>` 链接）
- [x] 1.3 添加编辑弹窗（Modal）HTML 结构

## 2. 样式调整

- [x] 2.1 添加编辑按钮样式
- [x] 2.2 添加弹窗 Modal 样式
- [x] 2.3 添加表单输入框样式

## 3. 核心逻辑实现

- [x] 3.1 实现 File System Access API 文件写入函数 `saveToJsonFile(data, filename)`
- [x] 3.2 实现编辑弹窗打开函数 `openEditModal(school, type)`
- [x] 3.3 实现保存函数 `saveSchool(school, type)` — 更新内存数据并写入 JSON
- [x] 3.4 实现取消函数 `closeEditModal()` — 关闭弹窗不保存

## 4. 事件绑定

- [x] 4.1 为每行的编辑按钮绑定点击事件
- [x] 4.2 为保存按钮绑定保存逻辑
- [x] 4.3 为取消按钮绑定关闭逻辑

## 5. 测试验证

- [ ] 5.1 验证区级学校编辑功能正常
- [ ] 5.2 验证市级学校编辑功能正常
- [ ] 5.3 验证网址正确显示为纯文本
- [ ] 5.4 验证浏览器兼容性提示（Firefox/Safari 不支持）
