# SHSEE 项目开发规范

## 代码修改前检查清单

修改元素ID、函数名或文件名之前，务必执行全局搜索：

```bash
grep -n "旧ID\|旧函数名\|旧文件名" index.html
```

确保所有引用点都已同步更新。

## 变量管理规范

- 全局变量统一在文件头部声明（`index.html` 的 `<script>` 顶部）
- 避免混用 `window.xxx` 和局部变量
- 修改变量名时使用 IDE 重构功能

## 小步验证原则

每个代码块修改完成后，验证：
1. 改动了哪些元素ID或函数名？
2. 哪些地方可能引用这些ID/函数？
3. 同步更新了所有引用点吗？

## 文档即代码

- 实施计划文档包含"实际Bug修复"章节
- 每次提交时顺便更新文档，而非事后补记
- 记录真实的实现细节，而非仅记录计划

## 功能验证清单

代码修改后，完整走查以下流程：

1. **Tab切换** — 验证各Tab切换正常
2. **数据加载** — 验证数据正确显示
3. **筛选搜索** — 验证筛选和搜索功能
4. **编辑保存** — 验证增删改操作
5. **跨模块交互** — 如地图点击跳转等

## 提交规范

- 每次修改后立即提交，避免积累
- commit message 使用中文描述修改内容
- 相关联的小改动可合并为一个提交
